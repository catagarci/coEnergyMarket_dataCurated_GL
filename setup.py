"""Setup"""
from setuptools import setup, find_packages

setup(
    name='extra',  # Nombre del paquete
    version='0.1',  # Versión del paquete
    packages=find_packages(),  # Encuentra todos los paquetes Python en el directorio actual
    install_requires=[
    ],
)
